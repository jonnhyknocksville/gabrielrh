
        <nav class="navbar align-items-start navbar-expand-lg bg-dark py-3 px-0 w-100">
            <div class="container-fluid bg-secondary-dark">
                
                <div class="logo-smartphone d-flex flex-column align-items-start logo ms-3">
                    <a aria-label="Retournez à la page d'accueil - Formation WS" href="{{ path('app_home') }}">
                        <img src="{{ asset('build/logo_tout_blanc.png')}}" width="auto" height="40px" title="Formation WS" alt="Formation WS">
                    </a>
                </div>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon">
                    <i class="text-secondary fas fa-bars" style="font-size:28px;"></i>
                    </span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <div class="full-screen d-flex align-items-center navbar-nav ms-auto">
                        <ul class="d-flex align-items-center navbar-nav">
                            <li class="nav-item px-1 py-1">
                                <a aria-label="Mon planning - Formation WS" class="smallBorderRadius px-3 bg-secondary text-light nav-link " href="{{ path('app_teacher_calendar') }}">Mon planning</a>
                            </li>
                            <li class="nav-item px-1 py-1">
                                <a aria-label="Mon profil - Formation WS" class="smallBorderRadius px-3 text-light nav-link " href="{{ path('app_profile') }}">Mon profil</a>
                            </li>
                            <li class="nav-item px-1 py-1">
                                <a aria-label="Ce déconnectez de mon compte - Formation WS" class="smallBorderRadius px-3 text-light nav-link " href="{{ path('app_logout') }}">Se déconnecter</a>
                            </li>
                        </ul>
                        
                    </div>
                </div>
            </div>
        </nav>