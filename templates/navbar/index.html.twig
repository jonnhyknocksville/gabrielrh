
        <nav class="navbar align-items-start navbar-expand-lg bg-dark py-3 px-0 w-100">
            <div class="container-fluid bg-secondary-dark">
                
                <div class="logo-smartphone d-flex flex-column align-items-start logo ms-3">
                    <a aria-label="Retourner à la page d'accueil de la réponse numérique" href="{{ path('app_home') }}">
                        <img src="{{ asset('build/logo_tout_blanc.png')}}" width="auto" height="40px" title="Formation WS" alt="Formation WS">
                    </a>
                </div>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon">
                    <i class="text-secondary fas fa-bars" style="font-size:28px;"></i>
                    </span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <div class="full-screen d-flex align-items-center navbar-nav ms-auto">
                        <ul class="d-flex align-items-center navbar-nav">
                            <li class="nav-item px-1 py-1">
                                <a aria-label="Commander un formateur - Formation WS" class="smallBorderRadius px-3 bg-secondary text-light nav-link " href="{{ path('professionals') }}">Commander un formateur</a>
                            </li>
                            <li class="nav-item px-1 py-1">
                                <a aria-label="Devenir un formateur - Formation WS" class="smallBorderRadius px-3 bg-primary text-light nav-link text-light" href="{{ path('teachers') }}">Devenir formateur</a>
                            </li>
                            <li class="nav-item px-1 py-1">
                                <a aria-label="Nos offres actuelles - Formation WS" class="smallBorderRadius px-3 fw-bold text-light bg-info nav-link text-info" href="{{ path('app_jobs') }}">Nos offres</a>
                            </li>   
                            <li class="nav-item px-1 py-1">
                                <a aria-label="Développer votre e-learning - Formation WS" class="fw-bold text-light nav-link text-info" href="{{ path('app_elearning') }}">Développer son e-learning</a>
                            </li>
                            <li class="nav-item px-1 py-1">
                                <a aria-label="Contactez-nous - Formation WS" class="nav-link text-light" href="{{ path('app_contact') }}">Contact</a>
                            </li>
                        </ul>
                        
                    </div>
                </div>
            </div>
        </nav>