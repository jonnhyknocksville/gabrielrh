
        <nav class="navbar align-items-start navbar-expand-lg bg-dark py-3 px-0 w-100">
            <div class="container-fluid">
                
                <div class="logo-smartphone d-flex flex-column align-items-start logo">
                    <a aria-label="Retourner à la page d'accueil de la réponse numérique" href="{{ path('app_home') }}">
                        <img src="{{ asset('build/logo_tout_blanc.png')}}" width="auto" height="40px" title="Académie WS" alt="Académie WS">
                    </a>
                </div>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon">
                    <i class="text-secondary fas fa-bars" style="font-size:28px;"></i>
                    </span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <div class="full-screen d-flex align-items-center navbar-nav ms-auto">
                        <ul class="d-flex align-items-center navbar-nav">
                            <li class="nav-item">
                                <a aria-label="Accéder aux formations vidéo de l'Académie WS" class="nav-link text-light" href="{{ path('app_who_we_are') }}">Qui sommes-nous?</a>
                            </li>
                            <li class="nav-item">
                                <a aria-label="Qui est l'Académie WS?" class="text-primary nav-link " href="{{ path('professionals') }}">Recruter un formateur</a>
                            </li>
                            <li class="nav-item">
                                <a aria-label="Contacter l'Académie WS" class="text-secondary nav-link text-light" href="{{ path('teachers') }}">Devenir formateur</a>
                            </li>
                            <li class="nav-item">
                                <a aria-label="Contacter l'Académie WS" class="nav-link text-info" href="{{ path('app_elearning') }}">Développer son e-learning</a>
                            </li>   
                            <li class="nav-item">
                                <a aria-label="Contacter l'Académie WS" class="nav-link text-light" href="{{ path('app_contact') }}">Contact</a>
                            </li>
                        </ul>
                        
                    </div>
                </div>
            </div>
        </nav>