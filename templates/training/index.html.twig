{% extends 'base.html.twig' %}

{% block meta_description %} Vous cherchez des formations professionnelles en bureautique, web, gestion de projet, RH et plus encore ? {{company_name}} propose des formations adaptées aux particuliers ainsi qu'aux entreprises de toutes tailles. {% endblock %}
{% block title %}Formations professionnelles pour le grand public et les entreprises {{parent()}} {% endblock %}

{% block body %}

    <div class="col-md-12 mx-auto row py-5" style="background:linear-gradient(0deg,#31092a,#0e0331)">

        <div class="col-md-12">
            <h1 class="text-center text-white"> ACCÉDEZ AUX MEILLEURES FORMATIONS PROFESSIONNELLES </h1>
            <h2 class="text-center text-primary fw400"> Des formations pour les particuliers et les entreprises de toutes tailles </h2>
        </div>

        <div class="col-md-12">
            <p class="text-center text-white"> BUREAUTIQUE <span class="text-primary">//</span> WEB <span class="text-primary">//</span> GESTION DE PROJET <span class="text-primary">//</span> RESSOURCES HUMAINES <span class="text-primary">//</span> FORMATION SUR-MESURE</p>
        </div>

        <div class="col-md-12 d-flex justify-content-center">
            <a aria-label="Comment ça marche pour suivre une formation - {{company_name}}" class="text-light smallBorderRadius colorWhite btn btn-secondary pull-right border-radius-btn mt-5 fw600 d-flex align-items-center" href="{{ path('app_professional_process') }}"><i class="fa-solid fa-gear me-2"></i>COMMENT ÇA MARCHE ?</a>
        </div>

    </div>

    <div class="row mb-5">
        <div class="row col-md-8 mx-auto mt-5">
            <div class="d-flex col-md-12 text-left">
                <h1 class="text-left color-grey"> <span style="color:black"> Vous êtes un particulier ou une entreprise. </span> </br> Pourquoi choisir nos formations ? </h1>
            </div>
            <div class="col-md-6 mb-5">
                <p class="text-justify p-0"> Chez {{company_name}}, nous proposons des formations professionnelles adaptées aussi bien au grand public qu'aux professionnels, qu'il s'agisse de petites, moyennes ou grandes entreprises. Nos formations couvrent des domaines clés comme la bureautique, le web, la gestion de projet, et les ressources humaines, offrant à chacun la possibilité de développer ses compétences et de s'adapter aux exigences du marché.</p>

                <a aria-label="Inscrivez-vous à une formation - {{company_name}}" href="{{ path('app_professional_process') }}" class="mt-3 btn btn-primary justify-content-center" style="color:white !important">JE SUIS UNE ENTREPRISE</a>
                <a aria-label="Inscrivez-vous à une formation - {{company_name}}" href="{{ path('app_professional_process') }}" class="mt-3 btn btn-secondary justify-content-center" style="color:white !important">JE SUIS UN PARTICULIER</a>
            </div>
        </div>

    </div>

    <section class="bg-secondary-dark justify-content-center align-items-center flex-column categories relative pb-5">


        <div class="text-with-gradient text-center py-5 relative">
            &lt; NOS FORMATIONS /&gt; 
        </div>

        <div class="row mx-auto col-md-10 p-3">

            {% for theme in trainings %}
                <div class="col-md-3 p-3">
                    <div class="card form-shadow bg-none">
                        <a aria-label="{{theme.name}} - {{company_name}}" href="{{path('app_get_training_details', {id:theme.id})}}">
                            <img class="card-img-top smallBorderRadius" src="{{ asset('build/themes/'~ theme.logo)}}" alt="{{theme.name}}" title="{{theme.name}}">
                        </a>
                        
                        <div class="card-body">
                            <a aria-label="{{ theme.name }} - {{ company_name }}" href="{{ path('app_get_training_details', {id:theme.id}) }}">
                                <h5 class="card-title fw-bold text-light">{{ theme.name }}</h5>
                                <p class="card-text mb-3 text-info">
                                    {{ theme.description|length > 200 ? theme.description|slice(0, 200) ~ '...' : theme.description }}
                                </p>
                            </a>
                        </div>

                    </div>
                
                </div>
            {% endfor %}

        </div>

    </section>
    
{% endblock %}
